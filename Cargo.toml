[workspace]
members = ["e-client-app",
    "e-client-basics",
    "e-client-bilingual",
    "e-client-config"]
resolver = "3"
[profile.release]
lto = true
codegen-units = 1
strip = true

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
